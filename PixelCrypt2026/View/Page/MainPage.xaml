<Page x:Class="PixelCrypt2026.View.Page.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:PixelCrypt2026.View.Page"
      xmlns:controls="clr-namespace:PixelCrypt2026.View.UserControl"
      mc:Ignorable="d"
      d:DesignHeight="720"
      d:DesignWidth="1280">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" 
              VerticalAlignment="Center">

            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel VerticalAlignment="Center">

                    <TextBlock Text="PixelCrypt"
                               FontSize="50"
                               Foreground="{StaticResource Foreground}"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Text="Encryption • Security • Utilities"
                               FontSize="15"
                               Foreground="{StaticResource Foreground}"
                               Margin="0,5,0,0"
                               Opacity="0.5"
                               HorizontalAlignment="Center"/>

                </StackPanel>

                <Separator Grid.Row="1"
                           Margin="120,5"/>

            </Grid>

            <Grid Grid.Row="1">
                <Border VerticalAlignment="Center">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                        <ItemsControl ItemsSource="{Binding Tools}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border>
                                        <controls:ToolCard/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>

        <Grid Grid.Row="2">
            <TextBlock Text="v1.0"
                       x:Name="Version"
                       Foreground="{StaticResource Foreground}"
                       FontSize="14"
                       Opacity="0.5"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>