<UserControl x:Class="PixelCrypt2026.View.UserControl.BasePageLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PixelCrypt2026.View.UserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800">

    <Grid Margin="10,5,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Назад"
                        Command="{Binding BackCommand}"
                        VerticalAlignment="Center"
                        Padding="15,5"/>

                <TextBlock Text="{Binding Title}"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           FontSize="30"/>
            </StackPanel>
        </Grid>

        <Separator Grid.Row="1"
                   VerticalAlignment="Center"
                   Height="2"/>

        <ContentPresenter Grid.Row="2"
                          Content="{Binding RelativeSource={RelativeSource AncestorType=local:BasePageLayout}, Path=PageContent}"/>
    </Grid>

</UserControl>