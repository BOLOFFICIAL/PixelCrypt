<UserControl x:Class="PixelCrypt2026.View.UserControl.BasePageLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PixelCrypt2026.View.UserControl">

    <Grid Margin="10,5,10,10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Margin="0,0,0,5"
                    Orientation="Horizontal">

            <Button Content="Назад"
                    Command="{Binding BackCommand,
                                      RelativeSource={RelativeSource AncestorType=local:BasePageLayout}}"
                    Padding="15,5"/>

            <TextBlock Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       FontSize="30"
                       Foreground="{StaticResource Foreground}"
                       Text="{Binding Title,
                                      RelativeSource={RelativeSource AncestorType=local:BasePageLayout}}"/>

        </StackPanel>

        <Separator Grid.Row="1"/>

        <ContentPresenter Grid.Row="2"
                          Content="{Binding PageContent, RelativeSource={RelativeSource AncestorType=local:BasePageLayout}}"/>

    </Grid>

</UserControl>